<!-- ตัวอย่าง: ใช้ ComplaintList ในหน้า Home -->
<!-- ==============================================================
import ComplaintList from '../components/complaints/ComplaintList';

// In your component
const [complaints, setComplaints] = useState([]);
const [loading, setLoading] = useState(true);

<ComplaintList 
    complaints={complaints}
    loading={loading}
    showFilters={true}
/> 
============================================================== -->